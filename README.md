# 🏛️ KazNU Lighthouse

Telegram бот для автоматических уведомлений о расписании занятий

## 🚀 Сетап

### Установка

```bash
git clone https://github.com/your-username/kaznu-lighthouse.git
cd kaznu-lighthouse
pnpm install
```

### Настройка

1. Скопируйте `env.example` в `.env`:

```bash
cp env.example .env
```

2. Добавьте токен бота в `.env`:

```env
BOT_TOKEN=your_telegram_bot_token_here
```
3. Запустите Redis:

```bash
redis-server
```

4. Запустите бота:

```bash
pnpm build
xvfb-run pnpm start
```

xvfb-run необходим для возможности запускать Playwright с Headed браузером на виртуальном дисплее. Univer не пускает headless браузеры лол

## Как юзать

1. /start в @kaznu_notifier_bot
2. Добавьте бота в чат.
3. Напишите `/settings` в чате (нужны права админа)
4. В личных сообщениях следуйте инструкциям, введите логин и пароль от Univer.
5. Для реконфигурации или удаления креденшлов с бота- также `/settings`

## Зачем?

Удобно следить за изменениями расписания (которые происходят слишком часто в этой шараге), удобно уведомляет за час до начала первого урока и за 10 минут до следующего. Бьет на группы уроков которые идут подряд, если урок за час- он уведомит также за час. 

Локализации нет, use english, it is such a shame not to know english nowadays. 

[WIP] Screenshots
